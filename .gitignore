.DS_Store
__pycache__/
*.pyc

# Sackmann raw cache. NOTE: formerly gitignored as "regenerable via download",
# but JeffSackmann's tennis_atp / tennis_wta repos were removed from GitHub
# (2026-06, only tennis_MatchChartingProject remains public). The cache is now
# the source of record, so it is committed. The engine falls back to it when the
# upstream download returns nothing.
# data/sackmann/

# Unified all_matches.csv (regenerable from the committed cache; ~37MB)
all_matches.csv

# Engine-migration backup snapshots (never commit)
*-bak

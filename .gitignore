.DS_Store
__pycache__/
*.pyc

# Raw match cache from LuckyLoser91/TennisCourtLog (both tours + players.csv).
# NOTE: JeffSackmann's tennis_atp / tennis_wta repos were removed from GitHub
# (2026-06, only tennis_MatchChartingProject remains public), so we migrated to
# the TennisCourtLog mirror (Sackmann history + tennis-data.co.uk live feed).
# The cache is committed as the source of record; the engine falls back to it
# when the upstream download returns nothing - the same resilience that the
# Sackmann takedown taught us to keep.
# data/matches/

# Unified all_matches.csv (regenerable from the committed cache; ~37MB)
all_matches.csv

# Engine-migration backup snapshots (never commit)
*-bak
